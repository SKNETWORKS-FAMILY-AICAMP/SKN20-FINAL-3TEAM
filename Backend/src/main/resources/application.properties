# ===============================
# ìë² ì¤ì 
# ===============================
server.port=8080

# ===============================
# 파일 업로드 설정 (도면 이미지)
# ===============================
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# ===============================
# PostgreSQL Datasource
# ===============================
# 1. DB 연결 URL (jdbc:postgresql://주소:포트/DB명 형식)
spring.datasource.url=jdbc:postgresql://my-postgres-db.c96ewaa4imoq.ap-northeast-2.rds.amazonaws.com:5432/postgres

# 2. 계정 정보
spring.datasource.username=postgres
spring.datasource.password=admin0113!

# 3. 드라이버 클래스 (생략 가능하지만 명시하는 것이 좋음)
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# 1. 하이버네이트 6 버전의 표준 Dialect를 사용 (가장 안전)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# 2. 실행 시마다 기존 테이블을 삭제하고 다시 생성 (데이터는 날아가니 주의!)
spring.jpa.hibernate.ddl-auto=create

# 3. 드라이버가 SQL 타입(JSON, Vector)을 인식하도록 강제
spring.jpa.properties.hibernate.jdbc.batch_size=10

# Hibernate 설정 (Vector 및 JSONB 지원을 위해 필수)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


 
# ===============================
# JWT ì¤ì  
# ===============================
jwt.secret=skn20-dev-secret-key-please-change-in-production
jwt.expiration=86400000 


# ===============================
# ë¡ê¹ ì¤ì 
# ===============================
logging.level.org.springframework.web=INFO
logging.level.com.example.skn20=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=OFF
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=OFF

# ===============================
# Spring Mail Configuration
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=wearae0113@gmail.com
spring.mail.password=wocr hmrm knpz zewx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ===============================
# Python CV Server
# ===============================
python.server.url=http://localhost:8000
